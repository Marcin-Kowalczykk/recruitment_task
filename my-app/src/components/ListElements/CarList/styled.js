import styled from 'styled-components'

export const ListContainer = styled.div`
  background: ${(props) => props.theme.color.listLightBg};
  border-radius: 1em;
  margin: 5vh;
  box-shadow: rgba(149, 157, 165, 0.2) 0px 8px 24px;
`
